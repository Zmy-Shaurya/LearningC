#include<stdio.h>

/*int main()
{
        int length;
        int breadth;
        scanf("%d",&length);
        scanf("%d",&breadth);
        printf("The area is equal to %d", length*breadth);
        return 0;
}}*/

int main()
{
        int radius;
        printf("enter radius");
        scanf("%d",&radius);
        int height;
        printf("enter height");
        scanf("%d",&height);
        printf("volume of cylinder is equal to %f meters square", 3.14*radius*radius*height);
        return 0;
}